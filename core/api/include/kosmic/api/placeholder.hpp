#pragma once
// core/api: stable, semantically-versioned public API surface. Everything
// outside core/ (orchestration, future bindings) depends on this only,
// never on internal module headers directly.
namespace kosmic::api {}
