# templates: composed, complete theories built from core/theory/ building
# blocks (ScalarField, ComplexField, U1GaugeField, ...). Each subdirectory
# produces a library satisfying the TheoryDynamics contract -- no binaries
# live here, those are in examples/.
add_subdirectory(free_scalar)
