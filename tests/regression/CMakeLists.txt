# Conserved-quantity regression tests (energy conservation, winding number
# stability) run against core/backend/cpu. These must exist before the CUDA
# backend is trusted — they are the correctness baseline for that port.
